        <div class="row">
            <div class="col-sm-12 col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">SUBMIT</h2></div>
                    <div class="panel-body">
                        <form class="create-update-form" action="burgers/create" method="POST">
                          <div class="form-group">
                            <label for="ca">Burger Name:</label>
                            <input type="text" id="ca" name="name">
                          </div>

                          <div class="form-group">
                            <label for="slee">Choices</label><br>
                            <input type="radio" name="devoured" value="0" checked> Yes                            
                            <input type="radio" name="devoured" value="1"> No<br>
                          </div>

                          <button type="submit" class="btn-primary">DEVOUR</button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-sm-12 col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">DEVOUR</h2></div>
                    <div class="panel-body">
                        <ol>
                          {{#each burgers}}
                            {{#unless this.devoured}}
                              <li>
                                {{this.burger_name}}

{{!--                                 <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                  <input type="hidden" name="devoured" value="true">
                                  <button type="submit">Pick Up Order</button>
                                </form> --}}
                                {{> burgers/pickup-order}}
                              </li>
                            {{/unless}}
                          {{/each}}
                        </ol>
                    </div>
                </div>
            </div>
            <div class="col-sm-12 col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">DEVOURED</h2></div>
                    <div class="panel-body">
                        <ol>
                          {{#each burgers}}
                            {{#if this.devoured}}
                              <li>
                                {{this.burger_name}}

{{!--                                 <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                  <input type="hidden" name="devoured" value="false">
                                  <button type="submit">New Order</button>
                                </form> --}}
                                {{> burgers/order-pickedup}}
                              </li>
                            {{/if}}
                          {{/each}}
                        </ol>
                    </div>
                </div>
            </div>
        </div>